const Header = (props) => {
    return (
        <header className="header">
            <div className="header-container container row">
                <img src="./img/header-img.svg" alt="Header card icon" />
                <h2 className="header-title">Memory Game</h2>
            </div>
        </header>
    );
};

export default Header;
